.sidebar {
  /* position: absolute;
  top: 0;
  left: 0; */
  width: 200px;
  padding-top: 40px;

  &__title {
    text-transform: uppercase;
    /* font-weight: 600; */
    letter-spacing: 2px;
    font-size: 12px;
    color: gray1;
    padding-left: 16px;
  }

  &__section {
    margin-bottom: 40px;
  }
}
